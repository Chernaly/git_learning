📅 日期：2026-02-09

------

## 🎓 侯捷 C++ 面向对象高级编程：第四课核心笔记

> **核心主题**：带指针成员的类设计 (Class with pointer members)
>
> **重点**：深拷贝 (Deep Copy) 与 内存管理三剑客 (The Big Three)

------

## 1. 为什么“带指针”的类很特殊？

如果类中包含指针（如 `char* m_data`），默认的拷贝行为是**浅拷贝 (Shallow Copy)**，即只复制指针地址。

- **后果**：多个对象指向同一块内存，修改互串，且析构时会发生“二次释放”导致程序崩溃。
- **对策**：必须手动实现**深拷贝 (Deep Copy)**。

------

## 2. 内存管理三剑客 (The Big Three)

只要类中涉及 `new` 申请内存，必须同时写好以下三个函数：

### ① 析构函数 (Destructor)

- **职责**：对象生命周期结束时，释放堆（Heap）内存。

- **代码**：

  C++

  ```
  ~MyString() { delete[] m_data; }
  ```

### ② 拷贝构造函数 (Copy Constructor)

- **触发时机**：`String s2(s1);` 或 `String s2 = s1;` (声明同时初始化)。

- **逻辑**：分配一块同样大小的新内存，再把内容拷过去。

- **代码**：

  C++

  ```
  MyString(const MyString& str) {
      m_data = new char[ strlen(str.m_data) + 1 ];
      strcpy(m_data, str.m_data);
  }
  ```

### ③ 拷贝赋值函数 (Copy Assignment Operator)

- **触发时机**：`s2 = s1;` (s2 已存在)。
- **四个标准步骤**：
  1. **自我赋值检查**：`if (this == &str) return *this;` (防止“自杀”)。
  2. **释放旧内存**：`delete[] m_data;`。
  3. **分配新内存**。
  4. **拷贝内容**。

------

## 3. 堆、栈与内存生命周期

| **内存位置**  | **英文**   | **特点**         | **析构时机**                     |
| ------------- | ---------- | ---------------- | -------------------------------- |
| **栈**        | **Stack**  | 函数内的局部变量 | 函数结束（右大括号 `}`）自动析构 |
| **堆**        | **Heap**   | `new` 出来的内存 | 必须手动 `delete` 才会析构       |
| **静态/全局** | **Static** | 程序启动即存在   | 程序结束时析构                   |

- **准则**：`new` 出来的东西如果不 `delete`，就会造成**内存泄漏 (Memory Leak)**。

------

## 4. 重点语法：自我赋值检查的深层意义

- **地址比较**：`this == &str` 比较的是两个对象的“房产证地址”。
- **安全性**：如果不检查，自我赋值时会先删掉自己，导致后续拷贝时源数据已丢失。
- **效率**：避免了多余的内存申请与拷贝动作。

------

## 💡 实践思考：OpenGL 中的应用

在处理 **OpenGL 纹理缓冲 (Texture Buffer)** 时，这些原则同样适用。如果你写了一个 `Texture` 类管理显存，必须在析构函数里释放显存 ID，否则显存会持续上升直到程序崩溃。

