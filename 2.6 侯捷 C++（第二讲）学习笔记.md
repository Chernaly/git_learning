📅 日期：2026-02-06

侯捷老师的《C++ 面向对象高级编程》第二集在第一集的基础上，更深入地探讨了 **“类（Class）的设计哲学”**，特别是围绕 **“复数类（Complex）”** 的实现过程，讲解了许多核心语法规范。

以下是针对第二集内容的重点总结：

------

## 📚 侯捷 C++ 面向对象高级编程（第二讲）学习笔记

### 1. 构造函数的特殊地位

侯捷老师详细讲解了构造函数的**“初值列（Initialization List）”**。

- **核心观点**：在构造函数的大括号 `{}` 之前使用 `: member(value)` 进行初始化，比在函数体内赋值效率更高。
- **底层原理**：初始化发生在变量被创建的同时；而赋值则是先创建（调用默认构造），再抹掉旧值赋予新值。这在处理大型顶点数组时性能差异显著。

### 2. 函数重载（Function Overloading）

- 在 C++ 中，同名函数只要参数个数或类型不同，就是不同的函数。
- 编译器会将这些函数名编排成不同的内部符号，从而实现灵活调用。

### 3. Singleton（单例模式）的实现

侯捷老师演示了一个非常经典的类设计技巧——如何让一个类只能创建一个对象。

- **方法**：将构造函数放在 `private` 区块中。
- **用途**：在你的 OpenGL 项目中，渲染引擎（Renderer）或窗口管理器通常就适合设计成单例。

### 4. 关键的参数与返回值传递准则

这是侯捷老师整门课程中最强调的**“金科玉律”**：

- **参数传递**：尽可能 **Pass by Reference to const**（引用传递）。这避免了数据的拷贝（尤其是面对复杂的 3D 模型顶点数据时）。
- **返回值传递**：如果可以，也尽量 **Return by Reference**。
  - *例外*：如果返回的是函数内部创建的临时变量（Local Object），则绝对不能返回引用，必须 **Return by Value**。

### 5. 常量成员函数（Const Member Functions）

- **规则**：如果一个成员函数不会修改类内的数据成员，**必须**在该函数声明后加 `const`。
- **原因**：如果你的类使用者定义了一个 `const Complex` 对象，但你的读取函数（如 `real()`）没加 `const`，编译器将报错，无法调用。

------

## 💡 结合信号处理与 CV 的思考

在这集课程中，侯捷老师通过 **`Complex`（复数）** 类的设计，展示了如何用 C++ 完美模拟数学概念。

- **信号处理联系**：复数在傅里叶变换、滤波器设计中是绝对的核心。你可以尝试自己写一个 `Complex` 类，并重载 `+`、`-`、`*` 等运算符。
- **CV 与 矩阵**：在计算机视觉中，我们处理的是一个个像素块或仿射变换矩阵。理解了 `Complex` 类的封装逻辑，你就能明白为什么 OpenCV 的 `cv::Mat` 设计得如此优雅。

### 📝 今天的练习建议：

尝试为你之前的三角形写一个类，并运用这节课学到的：

1. 使用 **Initialization List** 初始化顶点颜色。
2. 将获取颜色的函数设为 **const**。
3. 尝试将渲染函数设为 **public**，而将顶点数据设为 **private**。

**看完这两集，你是否感觉到 C++ 并不是简单的语法堆砌，而是一门讲究“工程美学”的艺术？接下来的第三集会涉及更复杂的运算符重载，需要我继续为你总结吗？**