📅 日期：2026-02-07


# 🎓 侯捷 C++ 面向对象高级编程：第三课核心笔记

> **核心主题**：运算符重载 (Operator Overloading)
> **设计目标**：让自定义类（如 Complex, Vector）在语法使用上与内置类型（int, double）对等。

---

## 1. 运算符重载的本质
在 C++ 中，运算符（如 `+`, `-`, `<<`）本质上都是**特殊的函数**。
- **编译器行为**：当你写下 `c1 + c2`，编译器会尝试寻找并调用名为 `operator+` 的函数。
- **命名规范**：函数名统一为 `operator` 后紧跟符号。

---

## 2. 成员函数版重载 (Member Function)
通常用于**修改自身**的操作（如 `+=`, `-=`, `*=`）。

### 🛠️ 代码示例 (以 += 为例)
```cpp
inline Complex& Complex::operator += (const Complex& r) {
    this->re += r.re; // 1. 利用“相同类互为友元”直接访问
    this->im += r.im;
    return *this;     // 2. 返回本体引用，支持链式操作 (c1 += c2 += c3)
}
```

### 💡 核心逻辑：

- **隐含参数**：左操作数（`this` 指针）由编译器自动传入，代表对象本体。
- **返回值**：必须返回 `Complex&`。目的是为了让 `c1 += c2` 的结果能继续参与下一次运算。

------

## 3. 全局函数版重载 (Non-member / Global Function)

通常用于**不修改自身**的操作（如 `+`）或处理**多种类型混合运算**。

### 🛠️ 代码示例 (以 + 为例)

C++

```
// 情况 1：复数 + 复数
inline Complex operator + (const Complex& x, const Complex& y) {
    return Complex(x.real() + y.real(), x.imag() + y.imag());
}

// 情况 2：双精度浮点数 + 复数 (必须用全局函数)
inline Complex operator + (double x, const Complex& y) {
    return Complex(x + y.real(), y.imag());
}
```

### 💡 核心逻辑：

- **传值返回 (Return by Value)**：这是**关键**！由于 `+` 产生的是一个新造出来的临时变量（Temp Object），函数结束即销毁。**绝对不能返回引用**。
- **临时对象语法**：使用 `Complex(r, i)` 直接创建并返回，效率极高。

------

## 4. 输出流重载 (operator <<)

这是 C 语言开发者最陌生的部分，但逻辑极为严密。

### 🛠️ 标准写法

C++

```
// os 对应 cout，x 对应你的复数对象
ostream& operator << (ostream& os, const Complex& x) {
    return os << "(" << x.real() << "," << x.imag() << ")";
}
```

### ❓ 为什么必须是全局函数？

- **直觉优先**：为了确保调用时是 `std::cout << c1`（屏幕在左，对象在右）。
- **反例**：如果写成成员函数，你将被迫写成 `c1 << cout`，这违反了人类的习惯。

### 💡 核心逻辑：

- **第一个参数**：必须是 `ostream& os`（代表输出管道）。
- **返回值**：必须返回 `os` 的引用。这保证了你可以连续打印：`cout << c1 << c2 << endl;`。

------

## 💡 第三课总结：如何选择重载方案？

| **运算符类型**        | **推荐位置** | **返回值类型** | **核心原因**             |
| --------------------- | ------------ | -------------- | ------------------------ |
| **`+=`, `-=`, `\*=`** | **成员函数** | `MyClass&`     | 修改自身，支持连加       |
| **`+`, `-`, `\*`**    | **全局函数** | `MyClass`      | 产生新物质，禁止返回引用 |
| **`<<`, `>>`**        | **全局函数** | `ostream&`     | 保持 `cout` 在运算符左侧 |

