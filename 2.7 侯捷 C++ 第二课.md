📅 日期：2026-02-07

# 🎓 侯捷 C++ 面向对象高级编程：第二课核心笔记

> **核心主题**：构造函数细节、函数重载与 this 指针底层逻辑
> **适用场景**：高性能图形类（如 Vector3/Matrix）的设计

---

## 1. 构造函数：初值列 (Initialization List)
这是第一课“金科玉律”的进阶理解。
- **底层差异**：
    - **初值列 `_x(x)`**：直接在成员变量内存分配时进行初始化（一步到位）。
    - **大括号赋值 `{_x = x}`**：先调用变量的默认构造函数，再进行赋值操作（两步操作，效率低）。
- **习惯规范**：成员变量习惯加下划线（如 `_x`），以便在初值列中清晰区分参数名与成员名。

---

## 2. const 关键字的精准定位
- **规则**：只要函数不改变类里的数据（如 `real()`, `imag()`），必须在函数签名后加 `const`。
- **如果不加的后果**：
  - 如果有人写了 `const Complex c1(1,2);`，那么他将无法调用你的 `c1.real()`。
  - 因为编译器认为 `real()` 可能会修改数据，而 `c1` 是只读的。

在 C++ 成员函数中，`const` 的位置决定了它的“法律效力”：
- **`float getX() const` (必须掌握)**：
    - `const` 放在函数签名后，修饰的是**整个函数**。
    - **承诺**：该函数绝不修改类中的任何成员数据。
    - **后果**：如果不加 `const`，常量对象（`const Vector v1`）将无法调用该函数，编译器会报错。
- **对比**：`const float getX()` 是修饰返回值，与函数是否能被常量对象调用无关。

***“数据要放在 private 里，参数传递和返回值尽量用 reference，不改数据的函数要加 const。”*** 

---

## 3. this 指针与成员访问
这是从 C 语言结构体迈向 C++ 类的核心桥梁。
- **`this` 的本质**：它是一个**指针**，永远指向“当前正在调用该函数的对象地址”。
- **访问语法**：
    - 因为 `this` 是指针，所以必须用 `this->_x` (对应 C 语言的 `ptr->member`)。
    - 在类内部，`this->` 通常可以省略，编译器会自动补全。
- **`*this` 的含义**：
    - 在 C++ 中，`*` 是取值符。`*this` 代表**对象本体**。
    - **链式调用**：通过 `return *this;` 返回对象引用，可以实现 `v1.add(v2).add(v3)` 这种连贯操作。

---

## 4. 函数重载 (Function Overloading)
C++ 允许同一个类里有多个同名函数，只要它们的参数类型或个数不同。

- **原理**：编译器在编译后，会把函数名“改写”成包含参数信息的内部符号。
- **应用场景**：你可以定义多个构造函数。例如：
  - `Complex()`：创建一个默认的 (0,0) 复数。
  - `Complex(double r)`：创建一个只有实部的复数。
- **注意**：如果两个函数在调用时会产生歧义（例如带默认参数的函数），编译器会报错。


---

## 5. 返回值传递：Return by Reference 的禁忌
- **Return by Value (传值返回)**：会把对象拷贝一份。
- **Return by Reference (传引用返回)**：返回的是对象本身。
- **死亡禁忌**：**绝对不能返回局部变量（Local Object）的引用。**
  - *形象比喻*：你在函数里造了一个房子，函数结束时房子拆了，你却给了别人一把房子的钥匙。别人去开门，程序就崩了。
- **侯捷准则**：只要函数返回后，那个对象还在（比如修改的是传入的对象），就尽量返回引用。

---

## 💡 实践心得：重构 Vector3 类
```cpp
class Vector3 {
public:
    // 1. 初值列 + 构造函数重载
    Vector3(float x=0, float y=0) : _x(x), _y(y) {}

    // 2. const 成员函数：承诺不改数据
    float getX() const { return _x; } 

    // 3. 返回引用 (*this)：支持链式加法
    Vector3& add(const Vector3& v) {
        _x += v._x;
        _y += v._y;
        return *this; 
    }

private:
    float _x, _y;
};