📅 日期：2026-02-06

侯捷老师《C++ 面向对象高级编程》的第三集主要围绕 **“运算符重载（Operator Overloading）”** 展开。这是 C++ 让自定义类（如之前的 `Complex`）像内置类型（如 `int`、`float`）一样好用的关键技术。

以下是第三集核心知识点的深度总结：

------

## 📚 侯捷 C++ 面向对象高级编程（第三讲）学习笔记

### 1. 运算符重载的本质

- **语法糖**：运算符重载本质上是函数调用。当你写 `c1 + c2` 时，编译器其实是在调用一个名为 `operator+` 的函数。
- **分两类实现**：
  1. **成员函数版本**：操作符左侧的对象是该类的实例（隐含 `this` 指针）。
  2. **非成员函数（全局）版本**：用于处理左侧不是该类对象的情况（例如 `double + Complex`）。

### 2. 参数传递与返回值的再强调

- **参数传递**：为了效率，**一律使用引用（Reference）**。如果函数内部不修改参数，必须加 `const`。
- **返回值传递**：
  - **可以返回引用**：如 `+=` 运算符，因为它修改的是左操作数本身，返回 `*this` 的引用即可。
  - **绝对不能返回引用**：如普通的 `+` 运算符，因为它在函数内部产生了一个新对象，函数结束时该对象被销毁，必须 **Return by Value**。

### 3. 操作符重载的特殊案例：`+=` 与 `+`

- **成员函数实现的 `+=`**：

  C++

  ```
  inline complex& complex::operator += (const complex& r) {
      this->re += r.re;
      this->im += r.im;
      return *this; // 支持连续赋值 c3 += c2 += c1
  }
  ```

- **非成员函数实现的 `+`**：为了支持多种组合（实数+复数、复数+实数、复数+复数），侯捷老师推荐将 `+` 设计为全局函数。

### 4. 输出操作符 `<<` 的重载（重点）

- **必须是全局函数**：因为 `cout << c1` 中，左侧的对象是 `ostream` 而不是你的 `Complex` 类。
- **函数签名**：`ostream& operator << (ostream& os, const complex& x)`。
  - **为什么返回 `ostream&`？** 为了支持链式调用，例如 `cout << c1 << c2;`。

### 5. 临时对象（Temp Object）

- 侯捷老师提到了 `typename()` 这种创建临时对象的方法。在重载 `+` 时，经常会直接返回 `complex(re, im)`，这会创建一个无名的临时对象，随后被拷贝返回，效率很高。

------

## 💡 结合信号处理与 OpenGL 的思考

### 📡 信号处理：滤波器系数的运算

在信号处理中，你经常需要对一组滤波器系数（Complex Array）进行加减乘除。

- **应用**：如果你重载了复数类的 `*` 和 `+`，那么实现一个 **FIR 滤波器** 的代码将从复杂的嵌套循环变成简洁的数学表达式，极大地提高了代码的可读性和维护性。

### 🎨 OpenGL：颜色与向量的混合

在你的三角形项目中，颜色是用 `vec4` 表示的。

- **底层原理**：在 GLSL 着色器语言中，你可以直接写 `color1 + color2`。这在底层其实就是 C++ 的运算符重载。
- **练习建议**：试着为你的 C++ 程序写一个简单的 `Color` 类，重载 `*` 运算符来实现颜色的乘法（例如将一个颜色变暗 50%：`myColor * 0.5f`）。

------

### 📝 记录到你的 Markdown 笔记：

Markdown

```
### ➕ 核心语法：运算符重载 (Operator Overloading)
- **设计哲学**：让自定义对象的操作像基础类型一样自然。
- **成员函数 vs 全局函数**：
    - `+=` 建议写成成员函数，返回 `*this`。
    - `<<` (输出流) 必须写成全局函数，否则只能写成 `c1 << cout` 这种怪异语法。
- **效率陷阱**：
    - 返回值是否能用 Reference？看对象是否是函数结束后依然存在的“活口”。
```

**这一集是 C++ “优雅感”的来源。你想继续观看第四集，了解 C++ 内存管理中最重要的“Big Three”（拷贝、赋值、析构）吗？这对处理大型顶点缓冲至关重要。**