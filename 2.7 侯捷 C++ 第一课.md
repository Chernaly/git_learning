📅 日期：2026-02-07


# 🎓 侯捷 C++ 面向对象高级编程：第一课核心笔记

> **核心主题**：良好的编程习惯与单一类（Class）的设计规范

---

## 1. 编程目标的两个层次
侯捷老师开篇明确了学习 C++ 的阶段目标，这决定了我们写代码的思维高度：
- **Object-Based (基于对象)**：学习如何设计一个**单一的类**（如 `Complex` 或 `String`）。
- **Object-Oriented (面向对象)**：学习如何处理**多个类之间的关系**（继承、复合、委托）。
*注：目前阶段我们重点攻克“基于对象”的设计，把每一个类写得“大气”且规范。*

---

## 2. 头文件规范 (Header File Design)
一个专业 C++ 程序员的头文件必须包含**防卫式声明**，防止重复包含导致的编译错误。

```cpp
#ifndef __MY_CLASS__
#define __MY_CLASS__

// 类声明 (Declaration)
class MyClass {
    // ...
};

#endif
```



## 3. 类设计的“金科玉律” (Golden Rules)

这是第一课中最具实战价值的四个规范：

### A. 构造函数必须使用“初值列” (Initialization List)

- **规范写法**：`Complex(double r, double i) : re(r), im(i) {}`
- **为什么要这样做**：初值列是真正的“初始化”，在大括号内赋值（`re = r`）则是“先创建再抹掉重写”。
- **性能意义**：在 OpenGL 这种每秒需要刷新 60 次以上的场景中，使用初值列能减少不必要的 CPU 开销。

### B. 参数传递优先使用引用 (Pass by Reference to const)

- **规范写法**：`void func(const MyClass& param)`
- **核心价值**：避免了大对象的内存拷贝。在处理复杂的顶点数据或图像帧（CV）时，传引用能极大提升速度。加 `const` 是为了保证数据安全。

### C. 返回值尽量使用引用 (Return by Reference)

- **原则**：如果函数返回的不是函数内部产生的临时变量，就应该返回引用。
- **禁忌**：绝对不能返回局部变量的引用（函数结束它就消失了）。

### D. 不修改数据的成员函数加 const (Const Member Functions)

- **规范写法**：`double real() const { return re; }`
- **重要性**：如果你不加 `const`，当别人定义一个常量对象（`const Complex c1`）时，将无法调用这个函数。

------

## 4. 数据封装 (Data Encapsulation)

- **准则**：数据（Data）必须放在 `private` 中，保护起来。
- **准则**：接口（Functions）放在 `public` 中，供外部调用。

------

## 💡 实践思考：重构我的 OpenGL 颜色管理

在 OpenGL 的 `main.cpp` 中，我们可以按照上述规范封装颜色：

C++

```
class TriangleColor {
public:
    // 准则：初值列初始化
    TriangleColor(float r, float g, float b) : _r(r), _g(g), _b(b) {}

    // 准则：不改数据，加 const
    float r() const { return _r; }
    float g() const { return _g; }
    float b() const { return _b; }

private:
    float _r, _g, _b; // 准则：数据私有
};
```
### 🔑 核心语法拆解：const MyClass&
- **`&` (引用)**：传递的是对象的“别名”，避免了昂贵的内存拷贝，提升执行效率。
- **`const` (常数)**：限制函数对传入对象的修改权限，确保原始数据的安全性。
- **大师总结**：
    - 参数传递原则：**尽可能 Pass by reference to const**。
    - 除非是像 `int`, `char` 这种只有几个字节的小数据，否则一律使用该写法。
### ⚡ 重点补充：内联函数 (Inline Functions)

- **核心定义**：将函数体直接替换到调用处，消除函数调用的“跳入跳出”开销。
- **侯捷规范**：
    1. 在类内部定义的函数（In-class definition）自动成为 inline 候选。
    2. 外部定义的函数需要手动加 `inline` 关键字。
- **工程审美**：
    - 优点：执行效率极高。
    - 注意：最终是否真正 inline，取决于编译器的判断（函数太复杂则无法 inline）。
- **OpenGL 应用**：
    - 获取坐标（get）、设置颜色（set）这类只有一两行的小函数，务必保证其为 inline。


### 🔒 重点补充：构造函数私有化 (Singleton 模式)

- **常规思维**：构造函数必须是 public，否则无法创建对象。
- **大师思维**：将构造函数放在 `private` 区，可以剥夺外部创建对象的权利。
- **单例模式 (Singleton)**：
    - **定义**：保证一个 Class 只有一个实例，并提供一个访问它的全局接点。
    - **写法关键**：
        1. 构造函数私有化。
        2. 类内声明一个 `static` 的私有对象（唯一的种子）。
        3. 提供一个 `static` 的函数供外部获取该对象。
- **OpenGL/CV 应用**：
    - 显卡资源管理器、日志记录器（Logger）、窗口配置类，通常都应设计为单例。

### 🤝 重点补充：相同类互为友元 (Friendship between objects)

- **核心概念**：相同 class 的不同 objects 互为 friends（友元）。
- **大师原话**：在类的方法中，可以直接访问【同类其他对象】的 private 成员。
- **直观理解**：
    - 外部函数：必须通过 `public` 接口访问。
    - 内部兄弟：直接访问 `private` 变量，无需“客气”。
- **OpenGL/信号处理应用**：
    - 当你需要写 `Vector3 + Vector3` 或者 `Color * Color` 时，直接操作内部的 `x, y, z` 或 `r, g, b` 变量，效率最高且逻辑最顺。













```