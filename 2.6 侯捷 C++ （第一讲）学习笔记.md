📅 日期：2026-02-06

侯捷老师的《C++ 面向对象高级编程》第一级第一讲（Introduction）主要聚焦于 **“C++ 编程习惯”** 和 **“类（Class）的设计哲学”**。以下是针对第一集核心内容的总结：

------

## 📚 侯捷 C++ 面向对象高级编程（第一讲）学习笔记

### 1. 编程目标的两个层次

侯捷老师开篇明确了学习 C++ 的两个阶段，这和你目前的 OpenGL 学习非常契合：

- **级别一：基于对象 (Object-Based)**：学会设计单一的类（Class）。例如，如何设计一个完美的 `Complex`（复数类）或 `String`（字符串类）。
- **级别二：面向对象 (Object-Oriented)**：学会设计多个类之间的关系（继承、复合、委托）。例如，理解为什么 `VBO` 需要被 `VAO` 管理。

### 2. 类（Class）的分类

他将 C++ 的类分为两大阵营，这是新手最容易混淆的地方：

- **不带指针的类 (Class without pointer members)**：如 `Complex`。这类类通常不需要写析构函数，拷贝构造也很简单。
- **带指针的类 (Class with pointer members)**：如 `String`。这类类**必须**手动处理“Big Three”（拷贝构造、拷贝赋值、析构函数），否则会导致内存泄漏（你之前遇到的 `LINK 1104` 虽是配置问题，但内存管理是 C++ 后期的头号大敌）。

------

### 3. 代码布局与头文件规范

他强调了一个优秀 C++ 程序员的“洁癖”：

- **防卫式声明 (Header Guards)**：

  C++

  ```
  #ifndef __COMPLEX__
  #define __COMPLEX__
  // 类定义
  #endif
  ```

  *这能防止你在大型项目中（比如包含多个库时）重复包含头文件导致的编译错误。*

------

### 4. 类声明的优美结构 (Header Layout)

侯捷老师展示了一个标准类的组织方式：

1. **模板声明**：如果需要通用的数据类型。
2. **构造函数 (Initializers)**：强烈推荐使用**初始化列表 (Initialization List)**，而不是在函数体内赋值。
   - **原因**：初始化列表直接调用成员变量的构造，效率更高（这在信号处理这种高性能要求的场景下非常重要）。
3. **内联函数 (Inline Functions)**：在类内部定义的函数会自动成为内联。对于简单的获取坐标、颜色的函数，应该设为 `inline` 以提高运行速度。
4. **访问级别 (Access Control)**：
   - **Data (数据)**：永远设为 `private`。
   - **Functions (接口)**：通常设为 `public`。

------

### 5. 关键语法习惯

- **const 的使用**：如果一个成员函数不会改变类的数据（比如只是读取三角形的颜色），**必须**加 `const`。这不仅是安全，也是为了让你的类能被 `const` 对象调用。
- **参数传递 (Pass by Value vs. Reference)**：
  - **原则**：尽可能 **Pass by Reference to const**。这能避免大块内存（如顶点数组）拷贝带来的性能损失。

------

## 💡 结合你当前 OpenGL 学习的建议

侯捷老师在这一集中提到：**“学习 C++ 不仅仅是学语法，更是学一种思维模式。”**

1. **类比 VBO/VAO**：你现在写的 OpenGL 代码是过程式的（Procedural）。看完这一集，你可以尝试把你的三角形渲染逻辑封装成一个 `Triangle` 类。
2. **构造函数的初始化**：当你以后在类里保存纹理 ID 或缓冲 ID 时，养成用初始化列表的习惯。
3. **头文件保护**：既然你现在在处理 `glad.h` 和 `glfw3.h`，确保你的个人头文件也有防卫式声明。

**这节课的重点是“养成好习惯”。你准备好把你的 OpenGL 三角形逻辑封装成一个标准的 C++ 类（Class）了吗？我可以帮你起个头。**